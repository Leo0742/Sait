# QA Strategy Baseline

- Contract tests for OpenAPI and AsyncAPI schemas.
- Invariant tests at service boundaries (READY lock, QR invalidation, idempotent check-in).
- End-to-end happy paths for event day operations.
- Degraded mode simulation with offline queue replay.
